import { Controller } from '@nestjs/common';

// use this module when application gets bigger, to separate functionalities
// @Controller('shapes')
@Controller('shapesLater')
export class ShapesController {
    // transfer code here from main controller
}
